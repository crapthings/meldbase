{
  "name": "meldbase-workspace",
  "private": true,
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/crapthings/meldbase.git"
  },
  "packageManager": "pnpm@11.10.0",
  "scripts": {
    "check": "pnpm --filter @meldbase/client check && pnpm --filter @meldbase/client build && pnpm --filter @meldbase/react check && pnpm --filter @meldbase/react build && pnpm --filter @meldbase/example-realtime-todos check",
    "test": "pnpm --filter './sdk/*' test",
    "build:example": "pnpm --filter @meldbase/example-realtime-todos build"
  }
}
